@model IEnumerable<Zeal_Institute.Models.Discount>

@{
    ViewBag.Title = "ListDiscount";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    var count = 0;
}

<h4 class="py-3">List Discount</h4>

<div class="card shadow-sm table-wrapper table-responsive mb-3">
    <table class="display table table-flush" id="table_id">
        <thead class="thead-light">
            <tr>
                <th class="text-center">#</th>
                <th>Student</th>
                <th>Batch</th>
                <th>Course Price</th>
                <th>Discount Value</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                count++;
                <tr>
                    <td class="text-center">@count</td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ApplicationUser.FullName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Batch.Name)
                    </td>
                    <td class="text-success">
                        $@Html.DisplayFor(modelItem => item.CoursePrice)
                    </td>
                    <td class="text-danger">
                        @Html.DisplayFor(modelItem => item.DiscountValue)%
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>

@section Scripts {
    <script type="text/javascript">
        $(document).ready(function () {
            $('#table_id').DataTable();
        });
    </script>
}
